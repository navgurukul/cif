import { makeStyles } from "@mui/styles";

const useStyles = makeStyles((theme) => ({
  bottomRow: {
    alignItems: "center",
    background: "#04010B",
    padding: "32px",
  },
  image: {
    marginBottom: "16px",
  },
}));

export default useStyles;
